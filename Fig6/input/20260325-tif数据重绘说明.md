# tif数据重绘

土地利用类型图绘制说明：

1. 输入数据文件：从`hongkong_lcz.npy` 读取二维lcz数组，从`hongkong_1.tif` 读取地理元信息；
2. 输出数据文件： `hongkong_lcz_w2w.tif` 。

地形图绘制说明：

1. 输入数据文件：从`srtm_59_08_hgt.npy` 读取二维height数组，从`srtm_59_08.tif` 读取地理元信息；
2. 输出数据文件： `hongkong_hgt_srtm.tif` 。



脚本由claude的sonnet4.6模型直接生成。

